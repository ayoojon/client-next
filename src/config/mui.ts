import createMuiTheme from '@material-ui/core/styles/createTheme';
export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#448B8F',
      // contrastText: '#fff',
    },
  },
});
