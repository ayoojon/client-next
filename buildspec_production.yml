version: 0.2

env:
  variables:
    NEXT_PUBLIC_GOOGLE_KEY: 'AIzaSyCpL2DpvIHSnMLaeaxg5k4R7szgjrcBFxQ'
    NEXT_PUBLIC_GMAIL_CLIENT_ID: '77285414059-mmhfavaqcau3afa6m6nmih1ghjcq4pvf.apps.googleusercontent.com'
  #parameter-store:
  # key: "value"
  # key: "value"
  #secrets-manager:
  # key: secret-id:json-key:version-stage:version-id
  # key: secret-id:json-key:version-stage:version-id
  #exported-variables:
  # - variable
  # - variable
  #git-credential-helper: yes
#batch:
#fast-fail: true
#build-list:
#build-matrix:
#build-graph:
phases:
  install:
    #If you use the Ubuntu standard image 2.0 or later, you must specify runtime-versions.
    #If you specify runtime-versions and use an image other than Ubuntu standard image 2.0, the build fails.
    runtime-versions:
      nodejs: 14
      # name: version
      # name: version
    #commands:
    # - command
    # - command
  pre_build:
    commands:
      - echo Installing packages...
      - yarn
  build:
    commands:
      - echo NEXT.JS Build started on `date`
      - npx serverless
  # post_build:
  #   commands:
  #     - echo Build completed on `date`
  #     - aws s3 rm s3://epl.com --recursive
  #     - aws s3 cp --recursive ./build s3://epl.com
  # - command
  # - command
  #post_build:
  #commands:
  # - command
  # - command
#reports:
#report-name-or-arn:
#files:
# - location
# - location
#base-directory: location
#discard-paths: yes
#file-format: JunitXml | CucumberJson
#artifacts:
#files:
# - location
# - location
#name: $(date +%Y-%m-%d)
#discard-paths: yes
#base-directory: location
#cache:
#paths:
# - paths
